спеки
  картинки
  HTML
    случай парного тега
    случай одиночного тега
    случай вовсе не тега
  разбор шаблонов
  таблиц
  ...

особенности:
  ; : — dl/dt/dd
  ::::: — indent (на самом деле — тот же dd!)
  внутри pre, который с пробелами работает (инлайн?) форматирование
  <!-- This is a comment -->
  <pre> — жёсткий преформаттинг
  &entity; https://github.com/threedaymonk/htmlentities
  
компактные инспекты
инлайн html (хотя бы) проверить
всякие <nowiki>, __TOC и т.п.

квирки:
  http://en.wikipedia.org/w/index.php?title=Wikipedia:Sandbox&action=submit
  линки, по крайней мере внешние, не падают, если в строке только один [, и не пытаются съесть след.строку
    try_scan — если не вышло, просто съедает символ и повторяет цикл
  викилинки не едят вложенные квадратики, только картинки едят
    → а картинки, видимо, едят только один уровень! scan('[[', ']]', max_level = 2)

для v.0.2 — постараться не падать и давать какой-то разумный вариант ВСЕГДА

полезный апи результата:
  sections
    section.text
    section.paragraphs
  templates
  images
  links
  tables
  categories (выборка по links)
    какие ещё полезные links бывают?
  infoboxes

таблицы (для начала — просто выкусывание)
подробный разбор шаблона
 
; текст — тоже какой-то заголовок
